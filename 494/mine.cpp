#include <vector>

class Solution {
  public:
    int findTargetSumWays(std::vector<int>& nums, int target) {
      std::vector<int[2]> results={{-1, 0}};

      for (int i = 0; i < nums.size(); i++) {
        for (int j = 0; j < results.size(); j++) {
          int prevResult[2]  {*results[j]};

          
        }
      }
    }
  };